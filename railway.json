{
  "$schema": "https://railway.app/railway.schema.json",
  "build": {
    "builder": "NIXPACKS",
    "buildCommand": "composer install --no-dev --optimize-autoloader"
  },
  "deploy": {
    "predeployCommand": "php artisan migrate --force && php artisan config:cache && php artisan route:cache"
  }
}
